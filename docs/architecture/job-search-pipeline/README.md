# Job search pipeline — Doubow figures

Vector assets here are **Doubow-specific** (job-search terminology only). They mirror `JobSearchPipelineCoordinator` and scoring behavior in `backend/api_gateway/services/job_search_pipeline.py` and `services/jobs_service.py`.

| File | Purpose |
| --- | --- |
| `pipeline-stages.svg` | Five default stages in order |
| `scoring-and-feedback-loop.svg` | Catalog + profile → blended scores → Discover; outcomes loop back to learning |

Colors align loosely with the web app (teal headers, amber accents). Safe to edit in Figma/Illustrator or by hand.
