# apps/api (Phase A mirror)

This directory is a non-breaking mirror entrypoint for the target monorepo layout.

Current source of truth remains `backend/api_gateway/` during Phase A.
Use workspace scripts from this package to run/test the existing API safely.
