[pytest]
# Same layout as CI (PYTHONPATH=backend/api_gateway): imports like ``services.*`` and ``prompt_loader`` resolve.
# Run from this directory: ``cd backend/api_gateway && pytest``, or from repo root: ``pytest backend/api_gateway/tests`` (pytest discovers this ini upward).
pythonpath = .
