export function IntegrityBanner({ message }: { message: string }) {
  return <div className="rounded-md bg-bg-light-green px-3 py-2 text-sm text-primary-green border border-border-subtle">{message}</div>
}
